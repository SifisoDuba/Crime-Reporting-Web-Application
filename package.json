{
  "name": "fullstack-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run serve --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.14.3"
  }
}
